16. Break amount into smallest banknotes

Write a C program to read an amount (integer value) and break the amount into the smallest possible number of bank notes.
Test Data :
Input the amount: 375
Expected Output:
There are:
3 Note(s) of 100.00
1 Note(s) of 50.00
1 Note(s) of 20.00
0 Note(s) of 10.00
1 Note(s) of 5.00
0 Note(s) of 2.00
0 Note(s) of 1.00


















Got it 👍. Let’s break your C program into a clear **algorithm** and then design a **flowchart** representation.

---

## 📝 Algorithm for Currency Notes Program

1. **Start**
2. **Input** the total amount of money.
3. **Initialize** an array of notes: `{1000, 500, 200, 100, 50, 20, 10, 5, 2, 1}`.
4. **Loop** through each note in the array:
   - If the amount is greater than or equal to the current note:
     - Calculate `count = amount / note`.
     - Print `count` and the note value.
     - Update `amount = amount % note`.
5. **Repeat** until all notes are processed.
6. **End**

---

## 🔄 Flowchart (Textual Representation)

```
          ┌───────────────┐
          │     Start      │
          └───────┬───────┘
                  │
          ┌───────▼────────┐
          │ Input amount    │
          └───────┬────────┘
                  │
          ┌───────▼──────────────┐
          │ Initialize notes[]    │
          └───────┬──────────────┘
                  │
          ┌───────▼──────────────┐
          │ For each note in arr │
          └───────┬──────────────┘
                  │
        ┌─────────▼───────────────┐
        │ Is amount >= note?       │
        └───────┬─────────────────┘
                │Yes
                ▼
      ┌────────────────────────────┐
      │ count = amount / note       │
      │ print count & note          │
      │ amount = amount % note      │
      └───────────┬────────────────┘
                  │
                  ▼
          ┌───────────────────────┐
          │ Next note in array    │
          └───────────┬───────────┘
                      │
                      ▼
              ┌───────────────┐
              │      End       │
              └───────────────┘
```

---

✅ This flowchart shows the **decision-making process** (checking if the amount is greater than or equal to the note) and the **looping structure** until all notes are processed.

Would you like me to create a **visual flowchart diagram** (with shapes and arrows) so you can use it directly in your notes or presentation?